# v0.17 checkpoint — community workspace

Passed before moving to the next stage:

- Prisma structural validation and duplicate-field check;
- static TypeScript/TSX transpilation and local import resolution;
- 32/32 autonomous tests;
- UX truthfulness rules;
- no browser prompt/confirm dialogs in the workspace;
- community → workspace → publication routes are present;
- inherited permissions are downward only;
- content actions require a reason and are audit logged;
- reports become official persistent topics instead of a second hidden content system;
- structure changes remain proposals until central administration resolves them.

Noise review: the workspace uses five task tabs and is not added to global navigation.
