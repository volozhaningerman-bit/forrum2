# v0.18 checkpoint — events and governance

Passed before moving to the next stage:

- public events page contains no creation forms;
- event creation and poll creation are separated from reading;
- event capacity, start time and publication state have autonomous rule tests;
- voting supports purpose, quorum, account age and advisory policy;
- a user can change a vote only while the poll is open;
- team actions are protected by inherited community permissions;
- attendance links lead to an event detail page and community context;
- 36/36 autonomous tests passed after this stage;
- static and UX validation passed.

Noise review: one global Events entry remains; events and polls are tabs inside the same hub rather than two new navigation items.
