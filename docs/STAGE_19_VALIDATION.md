# v0.19 checkpoint — projects, services and confirmed work

Passed before moving to the next stage:

- projects and services reuse one portfolio model instead of duplicate systems;
- only active and completed cards are public;
- drafts, paused and archived cards stay private to the owner;
- linked publications must belong to the portfolio owner;
- an interaction may reference a card only when the target user owns it;
- HTTPS links are deduplicated and limited to five;
- profile, project/service directories and interaction history link to one canonical card;
- project/service links were not added to global navigation;
- autonomous tests, static validation and UX truthfulness checks passed.

Noise review: portfolio is one profile tab and one personal-management link; public directories are reached contextually, not through extra header items.
